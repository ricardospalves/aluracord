/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'github.com',
      'www.alura.com.br',
    ],
  },
}

module.exports = nextConfig
